<!DOCTYPE html>
<html>
  <head>
    <title><%= Rails.configuration.application_name %></title>
    <% if Rails.env.development? %>
      <%= favicon_link_tag 'favicon_dev.ico' %>
    <% else %>
      <%= favicon_link_tag 'favicon.ico' %>
    <% end %>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://js.stripe.com/v3"></script>
    <%= tag.meta name: "stripe-key", content: Rails.application.credentials.stripe[:public_key] %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins&display=swap" rel="stylesheet">
  </head>

  <body
    style="font-family: 'Poppins', sans-serif;"
    class="flex flex-col min-h-screen subpixel-antialiased text-gray-900 bg-gray-50"
  >
    <%= render 'shared/navbar' %>
    <div class="flex-grow pt-24 pb-12">
      <%= yield %>
      <%= render 'shared/notice' %>
    </div>
    <%= render 'shared/footer' %>
  </body>
</html>
