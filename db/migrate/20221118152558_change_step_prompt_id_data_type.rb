class ChangeStepPromptIdDataType < ActiveRecord::Migration[6.1]
  def change
    # migration isn't needed, but safer to just comment out
    #add_column :task_results, :step_prompt_id, :string
  end
end
